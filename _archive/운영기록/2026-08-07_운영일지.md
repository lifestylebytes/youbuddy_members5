# 운영일지 · 2026-08-07 (금)

7기 Day 20 (마지막 날) · 8기 D-3

---

## 오늘 확정한 것

### 수료 기준

**18일 이상 학습 + 파이널 테스트 응시.** 지각도 1일로 그대로 인정.

Day 17 첫 공지에 "지각 0.5점"으로 나갔으나 되돌렸다. 이유는 두 가지.

1. 그 계산은 앱에 **한 번도 적용된 적이 없었다** (읽는 코드는 있는데 쓰는 데가 없는 키를 보고 있었음)
2. 멤버들은 17일 내내 "매일 인증하면 완주"로 알고 달려왔다

기수 중간에 기준을 조이지 않는다는 원칙으로 정리. 정정 공지 발송.

### 마감 폐지

학습도 파이널 테스트도 **영구 개방**. 기존 파이널 마감(마지막 Day + 3일)을 없앴다.
늦게 오신 분도 18일 채우고 응시하면 단어집 PDF 가 열린다.

### 없는 혜택 문구 제거

- Day 20 완주 카드의 `7기 수료 특가 · 완주 멤버 전용 · N시간 남음` → 삭제
  (실제 할인이 없는데 카운트다운이 돌고 있었고, 마감 계산도 8기 개강일이라 안내와 어긋남)
- 온보딩 Step 4 의 `7기 얼리버드 쿠폰` → 삭제
- Week 4 체크리스트 `얼리버드 자리 한정` → `8기 8/10 시작`

---

## 고친 버그

| 버그 | 원인 | 영향 |
|---|---|---|
| 홈 "파이널 테스트" 배너가 Week 4 복습으로 감 | `go('week-review')` 로 링크됨. 파이널은 별개 화면 | 배너로 들어간 멤버는 파이널을 볼 수 없었음 |
| 본인 이름에 완주 🏅 안 붙음 | `computeVerifyScore().score` 가 `.days` 로 바뀌어 undefined | 자기 화면에서만 미완주로 보임 |
| 파이널 "이미 봤다"고 표시 | `bingo_done['4']` 로 판정. 운영자 건너뛰기 버튼으로도 켜짐 | 운영자 계정 오표시 |
| 완주 메달이 시차 배지에 밀림 | 이름칸 폭 부족 | 완주자면 시차 숨기도록 변경 |
| 배포가 반영 안 됨 | **연속 푸시가 진행 중인 빌드를 취소** | 종일 반영 지연. 코드 문제가 아니었음 |

라벨도 정리: Week 4 주간복습 = `Week 4 테스트`, 진짜 시험 = `파이널 테스트`.

---

## 만든 것

| 산출물 | 위치 |
|---|---|
| Tracy 님 Week3~4 발표 피드백 | `_archive/7기/발표피드백/` + 앱 배포 |
| Silvia 님 Week4 발표 피드백 | `_archive/7기/발표피드백/` + 앱 배포 |
| 샘플 리포트 (가상 인물 Jamie) | `/sample-report.html` (공개) |
| 샘플 리포트 PDF 8p | `_archive/7기/카드뉴스/` |
| 운영자 리포트 목록 모달 | 프리미엄 탭 |
| 리포트 없는 멤버용 샘플 카드 | 프리미엄 탭 |
| 완주 메달 확인 SQL | `supabase_query_medal_check.sql` |

### 샘플 리포트 설정 근거

CRM 집계에서 영어 사용처 **회의 32명** (2위 이메일·메신저 14명).
페인포인트도 "말이 안 나와요", "회의에서 끼어들기 어려움", "같은 표현만 반복"이 반복 등장.
→ 가상 인물 Jamie (외국계 소비재 브랜드 마케팅, 아시아 리전 위클리 콜 주 2회) 로 구성.
→ 리포트 전체를 "회의에서 끼어들기" 하나로 관통시켜 읽는 사람이 자기 얘기로 읽히게.

**실제 멤버 리포트를 일반화해서 쓰지 않는다.** 7기 멤버끼리는 "멕시코 전시회" 한 줄로 알아본다.

---

## CRM 갱신

7기 프리미엄 사전 진단지 9명 전원을 노션 고객 CRM 에 반영 (신규 생성 없이 기존 카드의 `특성 메모` 에 추가).

기존 `페인포인트` 는 다른 시점 응답이라 덮어쓰지 않고, 비어 있는 항목만 채웠다.

> 멤버별 부담감 점수·개인 사정은 **`2026-08-07_멤버케어_비공개.md`** 에 있다.
> 그 파일은 `.gitignore` 로 커밋이 막혀 있고, 이 문서에는 개인 정보를 남기지 않는다.

한 가지 확인 필요한 건: 프리미엄 진단지를 제출하고 프리미엄 미팅에도 참여 중인데
CRM 옵션 필드는 베이직인 멤버가 한 명 있다. 실제 결제 내역 대조 필요.

---

## 스킬 갱신

`youbuddy-presentation-feedback` 을 두 번 업데이트했다.

1. **다음 기수 목표 섹션** 추가 (목표 3개 + 골드 박스, 측정 기준은 관찰 가능한 행동으로)
2. 버디 님이 직접 수정한 내용을 톤 규칙으로 역반영
   - 빈칸 퀴즈 정답은 **빈칸에 들어갈 단어만** (문장 전체 X)
   - 어미: `~더라고요` / `~잖아요?` / `~을 거예요` (단정 회피)
   - 공격적 동사 금지: 쥐다·누르다 → 신경쓰다·해소하다
   - Q&A 과장 금지: 같이 도달한 결론이면 "같이 결론을 내렸는데요"

---

## 남은 일

### 8기 (D-3)

- [ ] Supabase 8기 마이그레이션 SQL
- [ ] 8기 SYNONYM_GLOSSARY
- [ ] WEEK1_SIM 재작성
- [ ] MEMBER_DIRECTORY 확정 (결제 마감 후)
- [ ] 오늘 7기에 넣은 수정 사항 포팅 (파이널 마감 폐지, 리포트 카드, 학습/응시 문구, 메달 판정)
- [ ] D-2 고객센터 선안내 공지 발송
- [ ] D-1 입장 코드 발송 + 온보딩 공지 (프리미엄/베이직 각각, 대본 작성 완료)

### 7기 마무리

- [ ] Meeting 2 · 3 `recordingUrl` 비어 있음
- [ ] 수료자 최종 집계 → 단어집 PDF 배포
- [ ] 수료식 카드뉴스
- [ ] 8기 신청 버튼 링크가 아직 옛날 랜딩(`lifestylebytes.github.io`)을 가리킴

### 선택

- [ ] 운영자 파이널 미리보기가 실기록을 남기지 않게 분리
- [ ] mp3 → 주간 미팅 리포트 자동화 스킬 (아이디어 단계)
